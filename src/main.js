// Simple initialization script for the support site
console.log('LottoLedger Support Site loaded');

// You can add interactive features here as needed
document.addEventListener('DOMContentLoaded', () => {
  console.log('DOM fully loaded');
});
